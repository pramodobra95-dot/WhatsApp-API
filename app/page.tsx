/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import React from "react";
// Since this workspace is configured as a dual Vite + Next.js build, we point the App Router entry to our standard App page.
import App from "../src/App";

export default function Home() {
  return <App />;
}
